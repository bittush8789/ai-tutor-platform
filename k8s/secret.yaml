apiVersion: v1
kind: Secret
metadata:
  name: ai-tutor-secret
  namespace: ai-tutor
type: Opaque
stringData:
  # Replace with actual key or handle via sealed-secrets/vault in real prod
  GROQ_API_KEY: "gsk_replace_me_with_actual_key"
